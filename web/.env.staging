# Staging build overrides — used by:
#   npx vite build --mode staging --base=/staging/app/
# The staging SPA is deployed at https://gadaviral.com/staging/app/ and talks
# to the staging WordPress API in the same subdirectory.
VITE_API_BASE=/staging/wp-json/gadaviral/v1
